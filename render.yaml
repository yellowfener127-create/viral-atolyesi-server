# Repo kökünde olmalı. Otomatik deploy: Git bağlantısı + doğru branch + Dashboard’da Auto-Deploy açık.
services:
  - type: web
    name: viral-atolyesi-server
    runtime: node
    branch: main
    buildCommand: npm install
    startCommand: node server.js
    autoDeploy: true
